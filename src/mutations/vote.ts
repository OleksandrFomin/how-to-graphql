import { gql } from "@apollo/client";

export const VOTE = gql`
  mutation Vote($linkId: ID!) {
    vote(linkId: $linkId) {
      id
      link {
        id
        votes {
          id
          user {
            id
          }
        }
      }
      user {
        id
      }
    }
  }
`;
