overwrite: true
schema: "http://localhost:4000/"
generates:
  src/generated/types.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
